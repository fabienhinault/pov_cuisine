#include "colors.inc"  

// mur z=0 frigo et etageres
box {
  <0, 0, 0>,  // Near lower left corner
  <325, 250, -1>   // Far upper right corner
  texture {
    pigment { color White }
  }
//    rotate y*20     // Equivalent to "rotate <0,20,0>"
}
